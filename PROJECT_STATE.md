# 유니콘 KC 감사 프로젝트 상태

업데이트: 2026-08-02 (Asia/Seoul)

## 기준

- 판매자 ID: `A00214628`
- 전체 카탈로그: `2,230`개
- 최종 포함 기준: 상품상세 필수 표기 정보의 `저자, 출판사` 값이 정확히 `유니콘`
- 제외 KC: `U003E1577-7011`
- 차단/통신 오류 응답은 완료 처리하지 않음
- 최종 산출물: 검증된 No-KC 및 기간만료 KC XLSX

## 저장소 복구 사실

2026-08-02 확인 당시 `main`에는 `README.md` 한 줄만 존재했다. 이전 대화에서 저장소에 반영됐다고 보고된 `PROJECT_STATE.md`와 `diagnostics/*`는 실제 저장소에 없었다. 이 브랜치는 파일 라이브러리에 남은 2,230개 카탈로그 및 검증 진행본을 기준으로 복구한다.

## 이번 실행 결과

검증 기준을 모두 만족한 최종 행은 현재 각각 0건이다.

- No-KC: 0건
- 기간만료 KC: 0건

0건인 이유는 다음과 같다.

1. `productId=6732125611`은 상세의 `저자, 출판사=유니콘`이 확인됐지만 KC 번호 공란이 곧 실제 No-KC라는 뜻은 아니므로 제외했다.
2. `productId=1321324685`, KC `CB064H009-8001`은 상세 exact 및 KC 매핑은 있으나 SafetyKorea exact 상태를 확정하지 못해 기간만료 결과에서 제외했다.
3. `productId=9326155490`, KC `CB064H009-2001`도 SafetyKorea exact 상태 미확정으로 제외했다.
4. `productId=9334593133`, KC `CB064H009-4003`은 적합 근거가 있어 기간만료 결과에서 제외했다.
5. `CB064H009-9003`은 SafetyKorea에서 기간만료임이 확인되지만, 현재 확보 자료만으로 판매자 A00214628 상품의 상세 필드 `저자, 출판사=유니콘` exact와 동일 상품 KC 매핑을 동시에 확정한 행이 없어 최종 파일에 넣지 않았다.

## 생성 파일

- `유니콘_KC_No-KC_최종검증_20260802.xlsx`
- `유니콘_KC_기간만료_최종검증_20260802.xlsx`

두 파일 모두 결과 시트, 검증기준 시트, 제외·미확정 시트를 포함한다.
